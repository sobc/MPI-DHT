find_library(LIB_XXHASH xxhash)
if (NOT LIB_XXHASH)
  message("DHT: xxhash not found, skipping building tests")
else()
  add_executable(ReadWrite testReadWrite.c)
  target_link_libraries(ReadWrite PRIVATE LUCX_DHT ${LIB_XXHASH} ucx::ucp)
endif()
