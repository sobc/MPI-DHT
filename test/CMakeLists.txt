if (DHT_WITH_UCX)
  find_library(LIB_XXHASH xxhash)
  if (NOT LIB_XXHASH)
    message("DHT: xxhash not found, skipping building of tests")
  else()
    add_executable(ReadWrite testReadWrite.c)
    target_link_libraries(ReadWrite PRIVATE DHT_UCX ${LIB_XXHASH} ucx::ucp)
  endif()
endif()
